# Versions match the Colab image (freeze of 2026-08-06, Python 3.13.15).
# Pinning is a no-op in Colab and forces the same versions locally.
numpy==2.1.3
pandas==2.2.3
pyarrow==18.1.0
matplotlib==3.10.0
seaborn==0.13.2
scikit-learn==1.6.1
spacy==3.8.16
nltk==3.9.1
transformers==5.16.1
sentence-transformers==5.7.0
umap-learn==0.5.12
hdbscan==0.8.44

# not preinstalled in Colab, this is the only real install
bertopic==0.17.4

# NOT pinned on purpose: torch. Colab ships a matched CPU or CUDA build,
# and forcing a version there breaks the packages compiled against it.
